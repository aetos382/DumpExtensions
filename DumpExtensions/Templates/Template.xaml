<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:DumpExtensions"
    mc:Ignorable="d">

    <DataTemplate x:Key="MediumIcon" DataType="{x:Type local:InstalledExtension}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <TextBlock Text="MediumIcon" Grid.Row="0"></TextBlock>
            <TextBlock Text="{Binding Name}" Grid.Row="1"></TextBlock>
        </Grid>
    </DataTemplate>
    
    <DataTemplate x:Key="DetailView" DataType="{x:Type local:InstalledExtension}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <TextBlock Text="DetailView" Grid.Row="0"></TextBlock>
            <TextBlock Text="{Binding Name}" Grid.Row="1"></TextBlock>
        </Grid>
    </DataTemplate>

</ResourceDictionary>
